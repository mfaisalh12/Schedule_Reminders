// Kode di soal tidak valid karena mendeklarasikan variabel dengan const
// karena const merupakan deklarasi variabel yang statis
// Solusinya dengan mengganti dengan var atau let
for ( let i = 9 ; i < 10 ; i++ ) {
    console.log(i);
}